## GC
### Go GC 为什么是非分代、无整理、无压缩的？
    - Go 有极强的逃逸分析，大量对象直接栈分配，不进堆。
    - Go 内存分配器（mcache/mcentral/mheap）已经天然优化了短命对象，不需要 GC 再分代。
    - 分代 GC 复杂度高、写屏障复杂、收益有限，不符合 Go 简单、稳定、低延迟的设计目标。
Done
### 3. 并发三色标记扫描是什么？

#### 并发三色标记扫描（Concurrent Tri-Color Mark-and-Sweep）

这是现代垃圾回收器（如 Go 的 GC）中用于**在程序运行的同时**回收内存的核心算法。

##### 三种颜色的含义

| 颜色 | 含义 |
|------|------|
| ⚪ **白色** | 尚未被扫描 / 确认为垃圾（初始状态） |
| 🔘 **灰色** | 已发现，但其引用的子对象尚未扫描完 |
| ⚫ **黑色** | 已完全扫描，且其所有子对象也已处理 |

##### 算法流程

1. **初始阶段**：所有对象标记为白色（未扫描）
2. **发现根对象**：GC 扫描根对象，将直接可达的对象染为灰色
3. **扩展扫描**：逐步处理灰色对象，将其子对象染灰，自身变为黑色
4. **回收阶段**：扫描结束后，仍为白色的对象判定为垃圾，回收其内存

#### 为什么要"并发"？

传统 GC 需要**停止所有用户线程（Stop-The-World）**才能扫描内存，造成明显卡顿。三色标记算法允许 GC 线程和用户线程**同时运行**，但这带来了一个核心问题：

> 用户线程可能在 GC 扫描过程中**修改对象引用**，导致"应该存活的对象被错误回收"。

#### 两个关键问题（并发场景下）

##### 问题：对象丢失（漏标）

在 GC 扫描时，如果用户线程把一个白色对象的引用从一个灰色对象**移动**到黑色对象下，GC 就再也找不到这个白色对象了——它会被错误地当成垃圾回收。

##### 解决方案：屏障机制（Write Barrier）

| 屏障类型 | 原理 | 代表实现 |
|---|---|---|
| 插入写屏障 | 新建引用时，将目标对象染灰 | Dijkstra 屏障（Java） |
| 删除写屏障 | 删除引用时，将原对象染灰 | Yuasa 屏障 |
| 混合写屏障 | 插入 + 删除两种屏障结合 | Go 1.14+ |

#### Go 语言中的实际流程

Go 的 GC 将整个过程分为几个阶段，尽量缩短 STW 的时间：

1. **STW（很短）** — 开启写屏障，标记根对象
2. **并发标记** — GC 线程与用户线程同时运行，扫描灰色对象
3. **STW（很短）** — 关闭写屏障，处理残留灰色对象
4. **并发清扫** — 回收所有白色对象的内存

这样用户几乎感觉不到 GC 的存在，延迟通常控制在毫秒级以下。

### 4. go 如何实现的 并发三色标记扫描？
#### 4. Go 如何实现并发三色标记扫描？

Go 的 GC 并不是一次性完成所有工作，而是将整个过程拆分为多个阶段，穿插在用户程序运行之间，最大限度地减少停顿。

---

#### 整体阶段划分

Go GC 每轮回收经历以下四个阶段：

| 阶段 | 是否 STW | 主要工作 |
|------|----------|----------|
| Mark Setup | ✅ 是（极短） | 开启写屏障，初始化标记状态 |
| Mark（并发标记） | ❌ 否 | GC 与用户线程并发扫描对象图 |
| Mark Termination | ✅ 是（极短） | 关闭写屏障，处理残留灰色对象 |
| Sweep（并发清扫） | ❌ 否 | 惰性回收白色对象内存 |

STW（Stop-The-World）时间通常在 **0.1ms ~ 1ms** 以内。

---

#### Mark Setup 阶段（STW）

触发 GC 后，运行时做三件事：

1. **暂停所有 goroutine**（STW 开始）
2. **开启混合写屏障**（Hybrid Write Barrier）
3. **将根对象（Root）放入灰色队列**，根对象包括：
   - 全局变量
   - 每个 goroutine 的栈上变量
   - 寄存器中的指针

完成后立即恢复用户 goroutine（STW 结束）。

---

#### Mark 阶段（并发标记）

这是最核心的阶段。Go 启动若干 **GC Worker goroutine**，与用户 goroutine 并发运行。

##### GC Worker 的工作循环

```
取出一个灰色对象
  → 扫描它的所有指针字段
    → 若指向白色对象，将该白色对象染为灰色，放入队列
  → 将自身标记为黑色
重复，直到灰色队列为空
```

##### 灰色队列（Work Queue）的设计

Go 使用**分布式工作队列**来避免竞争：

- 每个 P（处理器）有自己的本地队列 `gcWork`
- 本地队列满时，溢出到全局队列
- GC Worker 优先消费本地队列，减少锁竞争

##### GC CPU 占用控制

Go 限制 GC Worker 最多使用 **25% 的 CPU**。当 GC 工作量大时，还会借用用户 goroutine 的时间片（`gcAssist`机制）——用户 goroutine 分配内存时，必须先帮 GC 完成一定量的标记工作，才能继续分配。

---

#### 混合写屏障（Hybrid Write Barrier）

并发标记期间，用户程序可能修改指针，破坏三色不变性。Go 1.14+ 通过**混合写屏障**解决此问题。

##### 三色不变性

GC 的正确性依赖以下不变性：
> **不能存在黑色对象直接指向白色对象**（否则白色对象会被错误回收）

##### 写屏障触发时机

每当用户代码执行指针赋值（`*slot = ptr`）时，运行时自动注入屏障代码：

```go
// 伪代码：混合写屏障逻辑
func writeBarrier(slot *pointer, ptr pointer) {
    shade(*slot)  // 将旧值（被删除的引用）染灰
    shade(ptr)    // 将新值（被插入的引用）染灰
    *slot = ptr
}
```

- **删除引用时染灰旧值**：防止被删除的对象因无人持有而漏标
- **插入引用时染灰新值**：防止黑色对象新引用的白色对象被漏标

##### 栈上对象的特殊处理

栈上的写操作**不加写屏障**（性能原因）。因此在 Mark Termination 阶段需对所有栈重新扫描一次，这是第二次 STW 的原因。

---

#### Mark Termination 阶段（STW）

当全局灰色队列为空时，进入终止阶段：

1. **再次暂停所有 goroutine**（STW 开始）
2. **重新扫描所有 goroutine 的栈**（因栈无写屏障，补偿处理）
3. **关闭写屏障**
4. **处理 finalizer 队列**
5. 恢复用户 goroutine（STW 结束）

---

#### Sweep 阶段（并发清扫）

清扫阶段完全并发，不需要 STW。

##### 惰性清扫（Lazy Sweep）

Go **不会在 GC 结束后立刻清扫所有白色对象**，而是采用惰性策略：

- 用户 goroutine 申请内存时，顺带清扫对应的 `mspan`
- 后台有专门的 `sweepg` goroutine 低优先级推进剩余清扫

##### mspan 的标记位

Go 的内存以 `mspan`（一组大小相同的内存块）为单位管理。每个 `mspan` 内有一个 `gcmarkBits` 位图：

- 标记阶段：将存活对象对应的 bit 置 1
- 清扫阶段：`gcmarkBits` 直接变成新的 `allocBits`（分配位图），bit 为 0 的槽位即可重新分配

这样清扫本质上只是**翻转位图**，极其高效。

---

#### GC 触发时机

Go 运行时通过 `GOGC` 环境变量（默认值 100）控制触发时机：

```
触发条件：当前堆大小 ≥ 上次 GC 后存活对象大小 × (1 + GOGC/100)
```

例如 `GOGC=100` 表示堆增长翻倍时触发，`GOGC=50` 表示增长 50% 时触发（更频繁但内存压力更小）。

Go 1.19+ 还引入了 `GOMEMLIMIT`，可以设置堆内存硬上限，运行时会自动调整 GC 频率以避免超出限制。

---

#### 完整流程总结

```
程序运行
  │
  ├─ 堆增长到阈值
  │
  ▼
[STW] Mark Setup
  开启写屏障 + 扫描根对象入灰色队列
  │
  ▼
[并发] Mark
  GC Worker 处理灰色队列（25% CPU）
  用户 goroutine 继续运行
  写屏障保护指针修改
  gcAssist 辅助标记
  │
  ▼
[STW] Mark Termination
  重扫栈 + 关闭写屏障
  │
  ▼
[并发] Sweep
  惰性清扫 mspan（翻转位图）
  │
  ▼
程序继续运行，等待下次触发
```
### 5. 强三色不变性和弱三色不变性的含义？
### 6. gc是为何需要写屏障？
### 7. 插入写屏障和删除写屏障的时机和区别？go中如何实现的·？
### 8. GC 的四个阶段？
### 9. 为何需要辅助标记和辅助清扫？
### 10. GC 4个阶段，STW发生在何时？
### 11. 描述下 gc 调步算法的实现？
### 12. 工作中gc debug的使用？
### 13. gc 清扫阶段 对象回收 和 内存单元回收的联系和差异？
### 14. 三色法为什么需要灰色
    - [refer](https://stackoverflow.com/questions/9285741/why-white-gray-black-in-gc#:~:text=2%20Answers&text=Gray%20means%20%22live%20but%20not,do%20a%20bit%20of%20marking.\)



## 垃圾回收
### 三色标记原理
### 插入写屏障
### 删除写屏障
### 写屏障 
### 混合写屏障
### GC 触发时机
### GO 语言中 GC 的流程是什么？
### GC 如何调优
